# Frontend

Placeholder. The React + TypeScript single-page app (login/MFA, accounts dashboard, transfers,
portfolio with live updates) is scheduled for Week 7. The directory exists now to lock the
monorepo layout decided in [ADR-0001](../docs/adr/0001-monorepo-layout.md).
